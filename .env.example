# Copy this file to .env before running the CLI.

# Required for `analyze`, `generate`, and `build`
OPENAI_API_KEY=sk-...

# Optional
# OPENAI_BASE_URL=https://api.openai.com/v1
# ANALYZE_MODEL=gpt-4o
# GENERATE_MODEL=gpt-4o-mini
