# 复制为 `.env` 后再运行 CLI。

# 仅在 `analyze` 与 `build --input/内联需求` 时需要
OPENAI_API_KEY=sk-...

# 可选
# OPENAI_BASE_URL=https://api.openai.com/v1
# ANALYZE_MODEL=gpt-4o
# OPENAI_TIMEOUT_SECONDS=30
# ANALYZE_MAX_TOKENS=2048
